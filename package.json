{
  "name": "@hagana/hagana",
  "version": "0.0.12",
  "description": "Runtime protection for NodeJS",
  "main": "dist/hagana.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "rm -rf dist && tsc && esbuild src/index.ts --bundle --platform=node --outfile=./dist/hagana.js  --watch",
    "build": "rm -rf dist && tsc && esbuild src/index.ts --bundle --platform=node --minify --outfile=./dist/hagana.js",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/app-root-path": "^1.2.4",
    "@vitest/ui": "^0.15.1",
    "nodemon": "^2.0.16",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4",
    "vitest": "^0.15.1"
  },
  "dependencies": {
    "app-root-path": "^3.0.0",
    "clean-stack": "^3.0.1",
    "error-stack-parser": "^2.1.4",
    "esbuild": "^0.14.47"
  }
}
